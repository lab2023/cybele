%p= t('mailer.salut', user: @resource.email)
%p= t('mailer.devise.reset_password.desc1')
%p
  = link_to t('mailer.devise.reset_password.change'),
            edit_password_url(@resource, reset_password_token: @token)
%p= t('mailer.devise.reset_password.desc2')
%p= t('mailer.devise.reset_password.desc3')
